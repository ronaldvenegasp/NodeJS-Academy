const firstName = "Ronald Guiovanni";
const lastName = "Venegas Pulido";

const calcAge = function (year, now) {
  return now - year;
};

// module.exports = {
//   firstName,
//   lastName,
//   calcAge,
// };

export default calcAge;
